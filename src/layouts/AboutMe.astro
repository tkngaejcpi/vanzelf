---
import Prose from "@components/Prose.astro";
import BasePage from "@layouts/BasePage.astro";
---

<BasePage
  metadata={{
    title: "關於我",
    description:
      "我係一個讀 data science 嘅大學生，但係唔多鍾意 data science 🫠，我會喺呢度發佈我感興趣嘅嘢，希望你會鍾意 🥰。",
  }}
>
  <Prose>
    <slot />
  </Prose>

  <!-- prefetch when loaded -->
  <script defer>
    import { prefetch } from "astro:prefetch";

    const target = ["/"];

    target.forEach((t) => prefetch(t));
  </script>
</BasePage>
